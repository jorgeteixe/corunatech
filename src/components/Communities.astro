---
import { getCollection } from 'astro:content';
import CommunityCard from './CommunityCard.astro';

const communities = await getCollection('communities');
---

<section class="py-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-white mb-4">
        Comunidades Tecnolóxicas da Coruña
      </h2>
      <p class="text-xl text-gray-300 max-w-3xl mx-auto">
        Descubre e conéctate coas comunidades tech locais. Desde programación ata metodoloxías áxiles, 
        aquí atopas o teu espazo na comunidade tecnolóxica galega.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {communities.map((community) => (
        <CommunityCard {...community.data} />
      ))}
    </div>

    {communities.length === 0 && (
      <div class="text-center py-12">
        <p class="text-gray-400 text-lg">
          Non se atoparon comunidades. Pronto engadiremos máis!
        </p>
      </div>
    )}
  </div>
</section>