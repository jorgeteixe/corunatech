name: Notify Weekly Events

on:
  schedule:
    # Every Sunday at 8:00 PM Europe/Madrid (19:00 UTC in winter, 18:00 UTC in summer)
    - cron: '0 19 * * 0'
  workflow_dispatch: # Allow manual trigger

jobs:
  notify-weekly-events:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get next week events
        uses: actions/github-script@v7
        id: get-events
        with:
          script: |
            const script = require('./.github/scripts/get-weekly-events.cjs')
            await script({github, context, core})

      - name: Send Telegram notification
        if: always() # Send even if no events
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          message: ${{ steps.get-events.outputs.message }}
          format: markdown
